# Internal Go Packages

This directory contains shared Go code that is private to the `boxedsnake` module. This is where business logic, shared domain models, and Kubernetes interaction logic should be placed. By Go convention, packages here cannot be imported by external projects.
